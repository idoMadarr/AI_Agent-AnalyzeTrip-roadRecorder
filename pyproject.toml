[project]
name = "ai-agent"
version = "0.1.0"
description = "Ai Agent for diagnostic waypoints and trip data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.128.0",
    "openai>=2.15.0",
    "uvicorn>=0.40.0",
]
